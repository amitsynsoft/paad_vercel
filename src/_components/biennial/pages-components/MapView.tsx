'use client'

export default function MapView({ data }: { data: any }) {
  return <div className="max-w-5xl h-[500px] w-full mx-auto my-16 flex flex-col md:flex-row text-center items-center justify-center gap-8 p-6 bg-gray-400">Map View</div>
}
